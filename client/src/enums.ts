export enum Route {
  HOME = '',
  USER = `user`,
  TODOS = `todos`,
  BOARDS = `boards`,
  LOGIN = 'login',
  PROFILE = 'profile',
  USER_PROFILE = 'user/profile',
}

export enum LoginFormType {
  LOGIN,
  SIGNUP,
}
